# 项目指令 — lovestory 语音陪伴

## 核心原则
- 我是个人自用 PWA 项目，代码在 `C:\Users\Administrator\Downloads\lovestory-module`
- 没有用户许可绝不推送到 GitHub 仓库，所有改动只留本地
- `lovestory (1)` 是旧备份，`lovestory-module` 是当前工作目录

## 通用工作规范（Karpathy 原则 + 隐私保护）
1. **谋定而后动：** 遇到含糊指令不瞎猜，要求澄清。权衡方案利弊后再执行。
2. **至简至上：** 交付最小可行成果，不堆砌不预判未来需求，不针对极端边缘情况做过度设计。
3. **精准微调：** 只改目标范围，不碰无关代码。发现旧代码不完美但只要不影响本次任务就忽略不改。
4. **目标导向执行：** 明确定义完工标准，完成后逐条验证闭环。
5. **严守隐私底线：** 不泄露任何个人隐私、敏感数据。

## 双文件同步提醒
本项目是 hybrid 架构（旧文件 + 模块文件并存）。修改旧文件时同步修改对应的模块文件：
- `js/voice.js` ↔ `js/modules/voice/voice.js`
- `js/chat.js` ↔ `js/modules/chat/chat.js`
- 同理其他旧文件 ↔ 模块文件
